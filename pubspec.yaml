name: termullog
description: TermulLog Delivery App

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6
  geolocator: ^10.1.0
  sqflite: ^2.3.0
  path: ^1.8.3
  image_picker: ^1.0.7
  image: ^4.1.3
  path_provider: ^2.1.2
  intl: ^0.18.1
  permission_handler: ^11.3.0
  geocoding: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

name: termullog
description: TermulLog App

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  image_picker: ^1.1.2
  geolocator: ^11.1.0
  geocoding: ^3.0.0
  path_provider: ^2.1.3
  intl: ^0.19.0
  image: ^4.2.0
  http: ^1.2.1
  shared_preferences: ^2.2.2
  gallery_saver_plus: ^3.2.0
  share_plus: ^9.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

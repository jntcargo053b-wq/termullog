<?xml version="1.0" encoding="utf-8"?>
<!--
  FIX: base-config TIDAK lagi mengizinkan cleartext.
  Semua trafik app default ke HTTPS.
  HTTP hanya diizinkan untuk domain tile map OSM dan LocationIQ
  yang memang belum mendukung HTTPS pada endpoint tile mereka.
-->
<network-security-config>

    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <domain-config cleartextTrafficPermitted="true">

        <domain includeSubdomains="true">
            staticmap.openstreetmap.de
        </domain>

        <domain includeSubdomains="true">
            maps.locationiq.com
        </domain>

    </domain-config>

</network-security-config>
